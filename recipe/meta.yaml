{% set name = "tdlpackio" %}
{% set version = "2.0.0b4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/NOAA-MDL/tdlpackio/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 488afba8f4c4bcd5ae52cbaaaa171615dcc3b8e83e3c553d349afc2b6d8fd382

build:
  skip: true  # [win or py<310]
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - cython                                 # [build_platform != target_platform]
    - numpy                                  # [build_platform != target_platform]
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - libtdlpack
  host:
    - python
    - cython >=0.29.16
    - setuptools >=61.0
    - numpy
    - pip
    - libtdlpack
  run:
    - python
    - pyproj
    - libtdlpack

test:
  imports:
    - tdlpackio
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/NOAA-MDL/tdlpackio
  summary: Python interface for reading and writing TDLPACK data.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - EricEngle-NOAA
